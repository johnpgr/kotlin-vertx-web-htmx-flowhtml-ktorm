package experiment.entities.meta

import experiment.entities.*
import org.komapper.core.dsl.Meta

val User = Meta.userEntity
val Session = Meta.sessionEntity
val Post = Meta.postEntity
val Follow = Meta.followEntity
val Favorite = Meta.favoriteEntity
val Comment = Meta.commentEntity
